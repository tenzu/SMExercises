import chapter10module as c10m

print(f"求解（1）：")
l = 3.5
A1 = 11.5 * 1e-4  # 扶强材截面积，题目中原为16.7
A2 = 0.22 * 2 * 75 * 20 * 1e4  # 坞墙截面积，带板宽度按0.22*2倍扶强材间距计算
I1 = 158 * 1e-8  # 扶强材截面惯性矩
I2 = 0.22 * 2 * 75 * 20**3 / 12 * 1e-8  # 坞墙截面惯性矩

# 计算扶强材和坞墙的形心坐标
y1 = 75.5 * 1e-2  # 查表得到扶强材形心高度
y2 = 20 / 2 * 1e-2  # 坞墙高度为板厚的一半
print(
    f"组合体形心坐标：{c10m.centroidofAreas(A1, y1, A2, y2):.3f} m = {c10m.centroidofAreas(A1, y1, A2, y2)*1e2:.2f} cm"
)
